# LabOps 研究日記機能 コードレビュー結果

## 1. レビュー概要

| 項目 | 内容 |
|---|---|
| 対象 | `C:\Users\Owner\Desktop\MyApp\LabOps` |
| 対象ブランチ | `main` |
| 初回レビュー対象 | `76dffcf`（`feat(研究スケジュール): 週・日表示と参加者を追加`）時点 |
| 再レビュー対象 | `9a85087`（`fix(研究日記): 長い本文が狭い画面で枠からはみ出す問題を修正`）時点 |
| 実施日 | 2026-09-05 |
| 対象機能 | 研究日記の一覧・作成・閲覧・編集・削除、公開範囲、Markdown、検索、添付ファイル、コメント、いいね |
| レビュー方法 | 実装・仕様書・nginx設定・既存テストの静的確認、および研究機能テストの実行 |

## 2. 結論

初回レビューで検出した4件はすべて修正されている。追加されたいいね機能についても、閲覧権限、1ユーザー1件の制約、付け外し、一覧・詳細表示、日記削除時の連動を確認し、新たな修正必須事項は検出されなかった。

プロジェクト全体の自動テスト246件、マイグレーション差分確認、Django system check、Ruffのlint・format checkはすべて成功した。現在の実装はレビュー対象範囲について受入可能と判断する。運用時は8.5節の添付配信設定に引き続き注意すること。

### 初回レビューの指摘件数

| 重要度 | 件数 |
|---|---:|
| P1（重大） | 2 |
| P2（中） | 1 |
| P3（軽微） | 1 |
| 合計 | 4 |

### 再レビューで新たに検出した指摘件数

| 重要度 | 件数 |
|---|---:|
| P1（重大） | 0 |
| P2（中） | 0 |
| P3（軽微） | 0 |
| 合計 | 0 |

## 3. 指摘事項

### REV-001：非公開日記の添付ファイルを認証なしで取得できる

- 重要度：**P1（重大）**
- 分類：アクセス制御、情報漏えい
- 該当箇所：
  - `nginx/default.conf:16-18`
  - `config/urls.py:15-17`
  - `research/models.py:75-77`

#### 内容

日記の詳細画面では、本人の日記または研究室内に公開された日記だけを取得するよう制御されている。しかし、添付ファイルの実体は `/media/diary/<日記ID>/<ファイル名>` に保存され、本番環境ではnginxが `/media/` 以下を直接配信する。

この配信経路では、Django側の日記公開範囲やログイン状態が検査されない。そのため、添付ファイルのURLを知っている利用者は、未ログインまたは閲覧権限がない状態でも非公開日記の添付ファイルを取得できる。

#### 影響

- 非公開の実験結果、画像、資料などが漏えいする可能性がある。
- URLがブラウザ履歴、アクセスログ、共有メッセージ、Refererなどから漏れた場合にアクセスされ得る。
- 画面側の404制御だけでは、非公開という仕様を満たせない。

#### 推奨修正

添付ファイル用の認証付きダウンロードビューを設け、次の条件を検査する。

1. 利用者がログインしていること。
2. 日記の作成者本人、または日記が研究室内公開であること。
3. 添付ファイルがURLの日記IDに属していること。

大きなファイルをDjangoから直接返したくない場合は、認可判定後に `X-Accel-Redirect` を使用してnginxに内部配信させる。実ファイルの保存場所は、外部から直接参照できない `internal` なlocationにする。

#### 追加すべきテスト

- 未ログイン利用者は非公開添付を取得できない。
- 他ユーザーは非公開添付を取得できない。
- 他ユーザーは公開日記の添付を取得できる。
- 作成者本人は自分の非公開添付を取得できる。
- 日記IDと添付IDの組み合わせが不一致の場合は404になる。

#### 対応（2026-09-05）

**対応済み**

- 認可付き配信ビュー `research.views.diary_attachment_download` を追加し、
  `GET /diary/<id>/attachments/<attachment_id>/file` で公開した。
  ログイン状態・日記の公開範囲・添付が当該日記に属するかを検査し、いずれかを満たさない場合は404を返す。
- `/media/` の直接公開を廃止した。`config/urls.py` の `static()` による開発用配信を削除し、
  `nginx/default.conf` の `location /media/` を `internal` にした。
  これにより開発・本番の両方で、ブラウザから実体URLを直接開けない。
- 本番では認可判定後に `X-Accel-Redirect` で nginx へ実体配信を委譲する
  （`settings.USE_X_ACCEL_REDIRECT`。未指定時は `DJANGO_DEBUG=0` で自動的に有効）。
- 詳細画面のリンクを `attachment.file.url` から `DiaryAttachment.download_url` に変更した。

### REV-002：任意形式の添付によるStored XSSの可能性がある

- 重要度：**P1（重大）**
- 分類：入力検証、コンテンツ配信、XSS
- 該当箇所：
  - `research/forms.py:37-53`
  - `nginx/default.conf:16-18`
  - `templates/research/diary_detail.html:50-58`

#### 内容

添付フォームは10MBのサイズ上限だけを検査し、拡張子やContent-Typeを制限していない。保存されたファイルはアプリと同じオリジンの `/media/` から配信され、詳細画面にはファイルを新しいタブで開くリンクが表示される。

このため、HTMLやSVGなどの能動的コンテンツを公開日記に添付し、別の利用者に開かせると、アプリと同じオリジン上でスクリプトが実行される可能性がある。

#### 影響

- ログイン中利用者の権限で、意図しない操作を実行される可能性がある。
- CSRFトークンや画面上の情報が取得される可能性がある。
- 公開日記を経由した研究室メンバーへの攻撃に利用され得る。

#### 推奨修正

- 業務上必要なファイル形式だけを許可する。
- 拡張子だけでなく、ファイルシグネチャや検証済みContent-Typeも確認する。
- 画像以外は `Content-Disposition: attachment` を指定してダウンロードさせる。
- `X-Content-Type-Options: nosniff` を設定する。
- 可能であれば、ユーザー添付ファイルをアプリとは別のオリジンから配信する。
- SVG、HTML、JavaScriptなどの能動的コンテンツは拒否するか、無害化できる場合だけ受け入れる。

#### 追加すべきテスト

- HTMLおよびSVGのアップロードが拒否される。
- 許可された画像・PDF・データファイルだけを登録できる。
- 非画像ファイルのレスポンスに強制ダウンロード用ヘッダーが設定される。
- クライアント指定のContent-Typeだけでは形式判定されない。

#### 対応（2026-09-05）

**対応済み（1点のみ代替手段を採用）**

- 形式判定を新規モジュール `research/attachments.py` に集約した。
- `DiaryAttachmentForm.clean_file` に、拡張子のホワイトリストとファイルシグネチャの二段検証を追加した。
  許可形式は画像（PNG / JPEG / GIF / WebP）・PDF・Office文書（xlsx / docx / pptx）・
  データファイル（CSV / TSV / TXT / MD / JSON / ZIP）。HTML・SVG・JavaScript は拒否する。
- クライアントが申告する Content-Type は判定に使わず、配信時の Content-Type も拡張子から決める。
- 画像だけを `Content-Disposition: inline` で表示し、それ以外は必ず `attachment` でダウンロードさせる。
- 全ての配信レスポンスに `X-Content-Type-Options: nosniff` を設定した（nginx側にも同ヘッダーを付与）。
- ファイル名からディレクトリ成分と制御文字を除去し、ヘッダー生成時の不正なレスポンスを防いだ。
- 「ユーザー添付をアプリとは別のオリジンから配信する」は**未対応**。
  学内単一ホスト運用で追加ドメインの用意が必要になるため、今回は
  形式制限・サーバー側Content-Type決定・強制ダウンロード・`nosniff` の組み合わせで代替した。

### REV-003：日記を削除しても添付ファイルの実体が残る

- 重要度：**P2（中）**
- 分類：データライフサイクル、ストレージ管理
- 該当箇所：
  - `research/views.py:179-183`
  - `research/models.py:88-96`

#### 内容

日記削除時は `DiaryEntry.delete()` だけを実行している。外部キーのCASCADEによって添付ファイルのDBレコードは削除されるが、Djangoの `FileField` はストレージ上のファイルを自動削除しない。

個別の添付削除処理では `attachment.file.delete(save=False)` を明示的に呼び出しているが、日記削除時には同等の処理がない。そのため、日記削除後もファイルの実体が `media/diary/` 以下に残る。

#### 影響

- 削除したはずの研究データがディスク上に残る。
- ストレージ使用量が増え続ける。
- REV-001の直接配信構成と組み合わさると、削除後も既知のURLから取得できる可能性がある。

#### 推奨修正

- 日記削除前に関連添付を列挙し、DBトランザクション完了後にファイルを削除する。
- ユーザー削除、管理画面からの削除、QuerySetの一括削除でも同じライフサイクルが保証される設計にする。
- ストレージ削除失敗時のログ記録と再試行方針を定める。

#### 追加すべきテスト

- 日記削除後、添付のDBレコードだけでなくファイル実体も存在しない。
- ユーザー削除時にも関連ファイルが削除される。
- 複数添付を持つ日記を削除すると、すべての実体が削除される。

#### 対応（2026-09-05）

**対応済み**

- `research/signals.py` に `DiaryAttachment` の `post_delete` レシーバーを追加し、
  `ResearchConfig.ready()` で登録した。
  添付削除ビュー・日記のCASCADE削除・ユーザー削除・管理画面からの削除・
  QuerySetの一括削除のすべてで同じライフサイクルが保証される。
- 実体の削除は `transaction.on_commit` でコミット後に行い、
  削除がロールバックされた場合にファイルだけ失われるのを防いだ。
- 削除に失敗した場合はファイル名を含めて `logger.exception` で記録する。
  残留ファイルはこの記録を頼りに後から回収する方針とした。
- ビュー側の明示的な `attachment.file.delete(save=False)` は、二重管理を避けるため削除した。

### REV-004：添付エラー後にコメント入力欄が表示されない

- 重要度：**P3（軽微）**
- 分類：エラー時の画面表示
- 該当箇所：
  - `research/views.py:201-206`
  - `templates/research/diary_detail.html:120-126`

#### 内容

添付フォームのバリデーションが失敗した場合、詳細テンプレートへ渡すコンテキストに `comment_form` が含まれていない。一方、テンプレートは常に `comment_form.body` を参照するため、10MB超過などの添付エラー後はコメント入力欄が表示されなくなる。

#### 影響

- エラー発生時だけ画面構成が変わり、コメントを入力できなくなる。
- ページを再読み込みすれば回復するが、利用者には理由が分かりにくい。

#### 推奨修正

添付エラー時の再描画コンテキストにも、空の `DiaryCommentForm` を渡す。併せてコメントと添付の関連データを通常の詳細表示と同じ条件でprefetchする。

#### 追加すべきテスト

- 10MB超過ファイルの送信後もコメント入力欄が表示される。
- 添付エラーメッセージと既存コメントが同時に表示される。

#### 対応（2026-09-05）

**対応済み**

- 添付エラー時の再描画コンテキストに、空の `DiaryCommentForm` を渡すようにした。
- あわせて `comments__author` を prefetch し、通常の詳細表示と同じ条件に揃えた。

## 4. 良好だった点

- 日記の作成時に、投稿データではなくログインユーザーを作成者として設定している。
- 他人の日記の編集・削除は、作成者条件付きの取得によって404になる。
- 公開日記と非公開日記の一覧・詳細表示条件が明確に分離されている。
- コメント削除をコメント投稿者または日記作成者だけに制限している。
- コメント追加、添付追加・削除などの更新処理をPOSTに限定している。
- Markdown変換後に `nh3` でタグ、属性、URLスキームを制限している。
- 検索処理は表示中の公開範囲を先に適用しており、検索による非公開日記の漏えいを防いでいる。
- 主要な正常系と権限制御について、自動テストが整備されている。

## 5. テスト実行結果

次のコマンド相当で研究アプリのテストを実行した。

```powershell
$env:USE_SQLITE='1'
$env:PYTHONDONTWRITEBYTECODE='1'
.\.venv\Scripts\python.exe manage.py test research.tests --verbosity 1
```

結果：

- 実行件数：132件
- 成功：132件
- 失敗・エラー：0件
- Django system check：問題なし
- 注意事項：`staticfiles` ディレクトリが存在しない旨の警告が1件表示されたが、研究日記機能のテスト結果には影響していない。

テスト成功は既存仕様に対する回帰がないことを示すが、REV-001からREV-004のケースは既存テストに含まれていない。したがって、テストがすべて成功していることだけをもって、本番運用上の問題がないとは判断できない。

## 6. 推奨対応順序

1. REV-001：添付ファイルを認可付き配信へ変更する。
2. REV-002：添付形式を検証し、危険な形式を拒否または強制ダウンロードにする。
3. REV-003：日記・ユーザー削除時にファイル実体も削除する。
4. REV-004：添付エラー時のテンプレートコンテキストを補完する。
5. 上記のセキュリティ・ライフサイクル・エラー表示テストを追加し、研究機能全体の回帰テストを再実行する。

## 7. 総合判定

初回判定：**要修正**

基本機能は動作し、コードにも適切な権限制御やテストが多く含まれている。ただし、非公開データを扱う機能としては添付ファイル配信のアクセス制御が不足している。REV-001とREV-002を解消し、その後に関連テストを追加してから本番運用へ進むことを推奨する。

再レビュー判定：**受入可能**

初回指摘4件の修正と、いいね機能を含む更新後の実装を確認した。新たな修正必須事項は検出されず、自動テストおよび静的検査もすべて成功した。

## 8. 対応結果（2026-09-05）

### 8.1 対応状況の一覧

| 指摘 | 重要度 | 対応状況 |
|---|---|---|
| REV-001：非公開日記の添付ファイルを認証なしで取得できる | P1 | 対応済み |
| REV-002：任意形式の添付によるStored XSSの可能性がある | P1 | 対応済み（別オリジン配信のみ代替手段） |
| REV-003：日記を削除しても添付ファイルの実体が残る | P2 | 対応済み |
| REV-004：添付エラー後にコメント入力欄が表示されない | P3 | 対応済み |

推奨対応順序（6章）のとおり、REV-001 → REV-002 → REV-003 → REV-004 の順に修正し、
最後に各指摘のテストを追加して研究機能全体の回帰テストを再実行した。

### 8.2 変更したファイル

| ファイル | 変更内容 |
|---|---|
| `research/attachments.py` | 新規。添付形式のホワイトリスト、シグネチャ判定、Content-Type決定、ファイル名の安全化 |
| `research/signals.py` | 新規。`post_delete` でストレージ上の実体を削除 |
| `research/apps.py` | `ready()` でシグナルを登録 |
| `research/forms.py` | `DiaryAttachmentForm` に形式検証を追加 |
| `research/models.py` | `DiaryAttachment` に `content_type` と `download_url` を追加、`is_image` を共通モジュール化 |
| `research/views.py` | `diary_attachment_download` を追加、添付削除の実体削除をシグナルへ移譲、添付エラー時のコンテキストを補完 |
| `research/urls.py` | 添付ダウンロードのルートを追加 |
| `config/urls.py` | 開発用の `/media/` 配信を削除 |
| `config/settings.py` | `USE_X_ACCEL_REDIRECT` を追加 |
| `nginx/default.conf` | `location /media/` を `internal` 化し `nosniff` を付与 |
| `templates/research/diary_detail.html` | 添付リンクを認可付きURLへ変更、許可形式の説明を更新 |
| `.env.example` | `DJANGO_USE_X_ACCEL_REDIRECT` を追記 |
| `research/tests/test_attachments.py` | REV-001〜004 のテストを追加（クラス分割） |
| `docs/04_実装/機能仕様書_研究室統合プラットフォーム.md` | 添付ファイルの仕様とエンドポイント表を実装に合わせて更新 |

### 8.3 追加したテスト

「追加すべきテスト」に挙げられたケースをすべて実装した。

- **REV-001（`DiaryAttachmentDownloadTests`）**：未ログイン・他ユーザーの非公開添付取得の拒否、
  他ユーザーによる公開添付の取得、本人による自分の非公開添付の取得、日記IDと添付IDの不一致で404、
  `/media/` がルーティングされないこと、詳細画面が認可付きURLを参照すること、
  `X-Accel-Redirect` 有効時のヘッダーと権限検査。
- **REV-002（`DiaryAttachmentFormatTests`）**：HTML / SVG / JavaScript の拒否、許可形式の登録、
  クライアント指定Content-Typeだけでは判定されないこと、拡張子偽装の拒否、
  非画像への強制ダウンロードヘッダーと `nosniff`。
- **REV-003（`DiaryAttachmentFileLifecycleTests`）**：添付単体の削除、日記削除（複数添付）、
  ユーザー削除、QuerySetの一括削除の各経路で実体が残らないこと。
- **REV-004（`DiaryAttachmentErrorRenderingTests`）**：サイズ超過後もコメント入力欄が表示されること、
  添付エラーメッセージと既存コメントが同時に表示されること。

### 8.4 再テスト結果

```powershell
$env:USE_SQLITE='1'
$env:PYTHONDONTWRITEBYTECODE='1'
.\.venv\Scripts\python.exe manage.py test
```

結果：

- 研究アプリ（`research.tests`）：161件（レビュー時点132件から29件追加）、全件成功
- プロジェクト全体：227件、成功227件、失敗・エラー0件
- `ruff check` / `ruff format --check`：問題なし
- Django system check：問題なし
- 注意事項：`staticfiles` ディレクトリが存在しない旨の警告は引き続き表示されるが、機能に影響はない

### 8.5 運用時の注意（破壊的変更）

- `/media/` の直接参照が無効になった。既存のブックマークや外部に共有された
  `/media/diary/...` のURLは404になる（非公開データを守るための意図した変更）。
  添付は詳細画面から取得する運用に変わる。
- Docker環境では `nginx/default.conf` の変更を反映するため、nginxコンテナの再作成が必要。
- `DJANGO_USE_X_ACCEL_REDIRECT` は未指定でよい（`DJANGO_DEBUG=0` の本番で自動的に有効）。
  リバースプロキシに nginx 以外を使う場合は `0` を明示し、Djangoから直接配信させる。

### 8.6 残課題

- REV-002 の「ユーザー添付をアプリとは別のオリジンから配信する」は未対応。
  学内単一ホスト運用のため追加ドメインの用意が前提になる。将来的に外部公開する場合は再検討する。
- 既存の添付ファイルのうち、レビュー前に許可対象外の形式で登録されたものは
  そのまま残る（配信時は強制ダウンロード＋`nosniff` になる）。必要なら別途棚卸しする。

## 9. 再レビュー結果（2026-09-05）

### 9.1 再レビュー対象

- 前回指摘REV-001〜REV-004の修正内容
- いいね機能（コミット `63d3906`）
- 長い本文が狭い画面で枠からはみ出す問題の修正（コミット `9a85087`）
- 上記変更による既存の研究日記機能への回帰

### 9.2 前回指摘の確認結果

| 指摘 | 確認結果 | 根拠 |
|---|---|---|
| REV-001：添付ファイルのアクセス制御 | 解消を確認 | 認可付きダウンロードビュー、日記と添付の組み合わせ検査、nginxの`internal`配信を確認 |
| REV-002：任意形式添付によるStored XSS | 解消を確認 | 拡張子ホワイトリスト、シグネチャ検査、非画像の強制ダウンロード、`nosniff`を確認 |
| REV-003：削除後にファイル実体が残る | 解消を確認 | `post_delete`と`transaction.on_commit`によるストレージ削除、および各削除経路のテストを確認 |
| REV-004：添付エラー後にコメント欄が消える | 解消を確認 | エラー再描画時の`comment_form`設定と回帰テストを確認 |

### 9.3 いいね機能のレビュー結果

次の点を確認した。

- いいね操作はPOSTに限定され、ログインが必須である。
- `_visible_diaries` を利用しており、他ユーザーの非公開日記にはいいねできない。
- ManyToManyの中間テーブルにより、同じユーザーのいいねが重複登録されない。
- もう一度押すといいねが解除され、HTMXでボタン状態、件数、いいねした人の表示が更新される。
- 一覧にはいいねがある場合だけ件数を表示し、詳細には現在の利用者の状態を`aria-pressed`で示す。
- 日記削除時は中間テーブルの関連データも削除される。
- モデル、ビュー、テンプレート、URL、マイグレーション、管理画面、仕様書が一通り更新されている。

再レビューでは、いいね機能について修正必須の不具合は検出されなかった。

### 9.4 長文表示修正の確認結果

一覧行の可変幅領域に`min-width: 0`を設定し、抜粋、タグ、投稿者名などが狭い画面内で折り返されるようCSSが追加されている。テンプレート側にも専用クラスが設定され、長い本文を使った回帰テストが追加されている。修正内容に問題は見つからなかった。

### 9.5 再検証結果

```powershell
$env:USE_SQLITE='1'
$env:PYTHONDONTWRITEBYTECODE='1'
.\.venv\Scripts\python.exe manage.py test --verbosity 1
.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run
.\.venv\Scripts\python.exe manage.py check
.\.venv\Scripts\ruff.exe check .
.\.venv\Scripts\ruff.exe format --check .
```

| 検証 | 結果 |
|---|---|
| プロジェクト全体テスト | 246件すべて成功 |
| マイグレーション差分 | なし（`No changes detected`） |
| Django system check | 問題なし |
| Ruff lint | 問題なし |
| Ruff format check | 68ファイルすべて整形済み |

テスト時に`staticfiles`ディレクトリが存在しない旨の警告が1件表示された。この警告は従来からのものであり、今回の機能テストや判定には影響しない。

### 9.6 最終判定

**受入可能**

前回指摘4件はすべて解消されている。いいね機能と長文表示修正にも、新たな修正必須事項は検出されなかった。自動テスト・マイグレーション確認・静的検査がすべて成功しているため、現在のレビュー対象を受入可能と判断する。
